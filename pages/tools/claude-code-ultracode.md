# Claude Code ultracode（総力戦モード）

> **TL;DR**: プロンプトに「ultracode」と書くか`/effort ultracode`を指定すると起動する[[tools/claude-code]]のモード。推論を最深レベルへ切り替え、タスク専用のオーケストレーションプログラムを自動生成し、最大16並行・合計1000体上限のサブエージェント編成を組む（@ren_aivest解説、v2.1.203以降）。

1体のAIと会話する働き方から、数十体の編成を采配する働き方への切り替えだと@ren_aivestは位置づける。プレイヤーの道具ではなく監督業の道具、という言い回しがこの機能の性格をよく表している——ユーザーの画面に返るのは編成の作業が終わった後の最終結論だけで、途中経過はプログラム側に蓄積される。

## 使い方と裏側の処理

使い方は2つ。プロンプト本文に「ultracode」という語を含めるか、`/effort ultracode` を指定する。裏では次の3段階が起きる。

1. 推論が最も深いレベルに切り替わる
2. そのタスク専用のワークフロー（オーケストレーションプログラム）が自動生成される
3. そのプログラムが最大16並行・合計1000体を上限とするサブエージェント編成を組む

`/goal`（完了条件を満たすまで自律的にターンを繰り返す機能、[[tools/claude-code-goal]]参照）と組み合わせるときは1行に同居させるだけでよい。

```text
/goal 記録アプリを公開する。全テスト通過とビルド成功とE2Eテスト通過。 ultracode
```

前半が検証込みの合格条件、末尾のultracodeが「この案件は編成を組んで総力戦でやれ」という体制の指定になる。合格条件で終わりを定義し、ultracodeで体制を定義する——1行の中に検収条件と戦力の指定が両方入る形だ。実際、316万インプレッションを集めた「Opus 5がFPSをワンショットで作った」という指示文（[[tools/claude-code-goal]]に原文と分解を記録）も、`/goal` と `ultracode` を同じ1行に同居させたものだった。

## 効く場面と効かない場面

@ren_aivestは、網羅が要る仕事（全件レビュー・監査・移行）や作業を独立に並列化できる案件でultracodeが効くとする一方、会話で詰めたい相談ごとや小さな修正1件に使うのはコストに見合わないと述べる。普段は`/goal`単体で回し、総力戦に値する案件だけ末尾に一語足す、という使い分けを紹介している。この判断基準は[[concepts/claude-code-dynamic-workflows]]の「workflowは万能ではなくトークンを大幅に多く消費する」という注意点、[[concepts/graph-engineering]]が紹介するAnthropic公式の「マルチエージェントは通常のチャット対話の約15倍のトークンを消費する」という数値とも整合する。

## 観察ログ（未検証）

- 2026-07-28 @ren_aivest: 「/goalはv2.1.139以降・ultracodeはv2.1.203以降の機能」という対応バージョンは、記事の出典節でClaude Code公式ドキュメント（code.claude.com/docs）に基づくとされているが、公式ドキュメント自体は未確認

## 問い

- ultracode実行時の実測トークン倍率は、[[concepts/graph-engineering]]が紹介するAnthropic公式数値（マルチエージェント約15倍）とどの程度一致するか
- 自分のwiki-ingestパイプライン（Tier判定・カテゴリ判定・インターリンク）のうち、独立並列化できる部分にultracodeを適用する価値はあるか

## 関連

- [[tools/claude-code-goal]] — `/goal`との組み合わせ実例（FPSデモの指示文分解）
- [[concepts/claude-code-dynamic-workflows]] — ultracodeが強制起動するオーケストレーション機構の本体（`agent()`/`parallel()`/`pipeline()`）
- [[concepts/graph-engineering]] — 複数サブエージェントを束ねる調整層としての位置づけ・コスト判断基準
- [[tools/claude-code]] — Claude Code本体
